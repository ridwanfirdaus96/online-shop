# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=shop_online

# For Railway MySQL, you can also use DATABASE_URL
# DATABASE_URL=mysql://user:password@host:port/database

# Application Settings
APP_ENV=production
APP_DEBUG=false

# Session Configuration
SESSION_LIFETIME=7200
